import { FocusDetectorDirective } from './focus-detector.directive';

describe('FocusDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusDetectorDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
