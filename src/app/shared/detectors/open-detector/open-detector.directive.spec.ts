import { OpenDetectorDirective } from './open-detector.directive';

describe('OpenDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenDetectorDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
