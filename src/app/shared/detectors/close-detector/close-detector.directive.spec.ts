import { CloseDetectorDirective } from './close-detector.directive';

describe('CloseDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseDetectorDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
