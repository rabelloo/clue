import { BlurDetectorDirective } from './blur-detector.directive';

describe('BlurDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurDetectorDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
